upstream backend {
    server 10.0.0.1;
}

http {
    server {
        listen 80;
        server_name example.com;

        location / {
            proxy_pass http://backend;
        }

        location /api {
            proxy_pass http://10.0.0.99:8080;
        }
    }
}

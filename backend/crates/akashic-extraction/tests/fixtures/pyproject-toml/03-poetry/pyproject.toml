[tool.poetry]
name = "poetry-app"
version = "3.1.4"
description = "A Poetry-managed project"

[tool.poetry.dependencies]
python = "^3.11"
requests = "^2.31"
rich = { version = "13.7", optional = false }
internal = { path = "../internal" }

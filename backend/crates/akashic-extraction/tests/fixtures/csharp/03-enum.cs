namespace MyApp.Models
{
    /// <summary>Represents the color primaries in the RGB model.</summary>
    public enum Color
    {
        Red,
        Green,
        Blue
    }

    /// <summary>Represents the status of an operation.</summary>
    public enum Status
    {
        Pending,
        Running,
        Completed,
        Failed
    }
}

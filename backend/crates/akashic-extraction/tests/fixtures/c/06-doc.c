/* not a doc comment — must be ignored */
/** Adds two integers. */
int add(int a, int b) {
    return a + b;
}

/// Doxygen triple-slash style.
/// Second line.
int sub(int a, int b) {
    return a - b;
}

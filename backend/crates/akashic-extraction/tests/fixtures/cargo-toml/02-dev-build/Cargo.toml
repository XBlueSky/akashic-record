[package]
name = "layered-crate"
version = "2.0.0"
edition = "2024"

[dependencies]
serde = "1.0"

[dev-dependencies]
criterion = "0.5"
proptest = { version = "1.4" }

[build-dependencies]
cc = "1.0"

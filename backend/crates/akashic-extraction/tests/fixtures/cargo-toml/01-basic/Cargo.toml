[package]
name = "basic-crate"
version = "0.3.1"
edition = "2021"

[dependencies]
serde = "1.0"
tokio = { version = "1.35", features = ["full", "macros"] }
anyhow = "1"

[features]
default = ["serde"]
